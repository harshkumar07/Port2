'use client';

import { useEffect, useState } from 'react';
import { ArrowLeft, Gamepad2, RotateCcw, Zap } from 'lucide-react';

export default function Games(){const [running,setRunning]=useState(false);const [score,setScore]=useState(0);const [time,setTime]=useState(10);const [x,setX]=useState(50);useEffect(()=>{if(!running)return;const t=setInterval(()=>setTime(v=>{if(v<=1){setRunning(false);return 0}return v-1}),1000);return()=>clearInterval(t)},[running]);const hit=()=>{if(!running)return;setScore(s=>s+1);setX(8+Math.random()*84)};const start=()=>{setScore(0);setTime(10);setX(50);setRunning(true)};return <main className="site-shell min-h-screen text-white"><div className="mx-auto max-w-6xl px-5 py-8 md:px-8"><a href="/" className="inline-flex items-center gap-2 text-xs text-white/45 hover:text-white"><ArrowLeft size={15}/> BACK HOME</a><div className="pt-24"><div className="eyebrow-small"><Gamepad2 size={13}/> PORTFOLIO ARCADE</div><h1 className="mt-4 text-[clamp(3.5rem,9vw,8rem)] font-semibold leading-[.82] tracking-[-.075em]">PLAY THE<br/><span className="gradient-text">SIDE QUEST.</span></h1><p className="mt-8 max-w-2xl text-white/45">Two tiny browser games. Zero tracking. Maximum unnecessary engineering.</p></div><div className="mt-12 grid gap-4 md:grid-cols-2"><section className="rounded-3xl border border-white/10 bg-white/[.025] p-6 md:p-8"><div className="flex items-center justify-between"><div><span className="project-kind">01 · REFLEX</span><h2 className="mt-2 text-2xl font-medium">Catch the Core</h2></div><Zap className="text-cyan-300/60"/></div><div className="mt-6 flex justify-between text-xs text-white/35"><span>SCORE <b className="text-white">{score}</b></span><span>TIME <b className="text-white">{time}s</b></span></div><div className="relative mt-4 h-72 overflow-hidden rounded-2xl border border-white/8 bg-[#02040a]" onClick={hit}>{running?<button aria-label="target" className="absolute size-10 -translate-x-1/2 -translate-y-1/2 rounded-full border border-cyan-200/70 bg-cyan-300/15 shadow-[0_0_35px_rgba(103,232,249,.7)]" style={{left:`${x}%`,top:`${20+((score*37)%65)}%`}}/>:<div className="absolute inset-0 grid place-items-center text-xs text-white/25">PRESS START</div>}</div><button onClick={start} className="btn-primary mt-4"><RotateCcw size={15}/> {running?'RESTART':'START GAME'}</button></section><section className="rounded-3xl border border-white/10 bg-white/[.025] p-6 md:p-8"><span className="project-kind">02 · MEMORY</span><h2 className="mt-2 text-2xl font-medium">Stack Recall</h2><p className="mt-4 text-sm leading-7 text-white/40">Remember the sequence of technologies. A simple memory game is coming next — the page is already part of the arcade.</p><div className="mt-8 grid grid-cols-4 gap-2">{['C#','NET','SQL','AI','JS','PY','AZ','WEB'].map((x,i)=><div key={x} className="grid aspect-square place-items-center rounded-2xl border border-white/8 bg-white/[.03] text-[10px] text-white/25 transition hover:border-violet-300/30 hover:bg-violet-300/5">{i%2===0?'?':x}</div>)}</div></section></div></div></main>}
